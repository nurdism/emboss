# Base URL of the link shortener Worker in worker/. Leave unset to hide the
# short link option and share full links instead.
VITE_SHORTENER_URL=https://emboss-shortener.nurdism.dev

# Absolute site URL, used to make the social card image URLs absolute at build.
SITE_URL=https://emboss.nurdism.dev/
